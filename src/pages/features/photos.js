import React from 'react';

import FeatureWrapper from '../../components/feature-wrapper';
import Layout from '../../primitives/layout';

export default () => (
  <FeatureWrapper>
    <Layout>
      <h1>Photos Feature</h1>
    </Layout>
  </FeatureWrapper>
);
